interface NavbarProps {}

const Navbar = (props: NavbarProps) => (
	<nav className="navbar navbar-dark bg-dark">
		<div className="container">
			<span className="navbar-brand mb-0 h1">Axios App</span>
		</div>
	</nav>
);

export default Navbar;
